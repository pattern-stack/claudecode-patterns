{
  "name": "my-plugin",
  "version": "0.1.0",
  "description": "What this plugin does",
  "author": "Your Name <you@example.com>",
  "homepage": "https://github.com/you/my-plugin",
  "license": "MIT",

  "_comment_defaults": "skills/, commands/, agents/, output-styles/ and hooks/hooks.json are scanned by DEFAULT — do not declare them unless you are pointing at a NON-default path. There is no `components` wrapper: every component field is top-level. See reference/plugins.md.",

  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/on-session-start.sh"
          }
        ]
      }
    ]
  },

  "mcpServers": {
    "my-server": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@example/my-mcp-server"]
    }
  }
}
