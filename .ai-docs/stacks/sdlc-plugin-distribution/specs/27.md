---
issue: 27
status: strategy-approved
related:
  - epic: 22
  - depends_on: [26]
gate_mode: auto
---

# Docs: README Quickstart, WORKFLOW, plugin reference, changelog

## Goal

Consolidate three narrative threads in a single docs pass:
1. Plugin install model (`cp -r` → `/plugin install` + `/sdlc:setup`).
2. Gate-modes opinion (strict / auto / "trust mode" + override layers).
3. Status taxonomy + `/sdlc:link-project`.

## File-level plan

### Modify

- `README.md` — Quickstart rewritten for plugin install. v1→v2 changelog gains "Install" + "Gate 1 mode" rows. New "Plugin development" section explaining dogfood symlinks. "What's in" section refers to `plugin/<dir>/`.
- `WORKFLOW.md` — already handled in PR 2 (Gate 1 section rewritten with strict/auto paths). Verify no `cp -r` references remain.
- `plugin/skills/claude-platform/reference/plugins.md` — section noting "this project not yet packaged" updated to "ships as the `sdlc` plugin" with install + setup pointer.

## Acceptance

- No `cp -r .claude/` references in README, WORKFLOW.md, or skill reference docs.
- Quickstart commands copy-paste to a successful install + setup in a fresh project.
- Changelog rows for "Install" AND "Gate 1 mode" present.
- "Plugin development" section shows the dogfood symlink layout.
- All slash-command references use namespaced `/sdlc:<cmd>` form.

## Out of scope

- Editing PR descriptions on already-merged PRs.
- WORKFLOW.md gates section (PR 2 handled).
